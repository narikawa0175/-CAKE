<!DOCTYPE html>
<html>
  <head>
    <title>Naganocake</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
   <header class="border border-dark">
    <% unless admin_signed_in? %>
    <% unless customer_signed_in? %>
     <nav class="navbar navbar-expand-lg navbar-light">
       <div class="navbar-brand border border-dark px-5 mt-5">
        <h1>LOGO</h1>
       </div>
       <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="navbarNavDropdown"
        aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
       </button>
       <div class="collapse navbar-collapse" id="navbarNavDropdown">
        <ul class="navbar-nav ml-auto">
         <li><%= link_to "About",about_path,class:'nav-link btn btn-outline-secondary mx-3 px-3' %></li>
         <li><%= link_to "商品一覧",items_path,class:'nav-link btn btn-outline-secondary mx-3' %></li>
         <li><%= link_to "新規登録",new_customer_registration_path,class:'nav-link btn btn-outline-secondary mx-3' %></li>
         <li><%= link_to "ログイン",new_customer_session_path,class:'nav-link btn btn-outline-secondary mx-3'%></li>
        </ul>
       </div>
     </nav>
     <div class="text-right mb-3 mr-5">
      <%= form_with url: root_path,method: :get,local: true do |f| %>
       <%= f.text_field :item,:placeholder=>"Search" do %>
        <%= f.submit :name=>nil %>
       <% end %>
      <% end %>
     </div>
    <% end %>
    <% end %>
   </header>
   <%= yield %>
   <footer class="border border-dark" style="height: 100px;"></footer>
  </body>
</html>
